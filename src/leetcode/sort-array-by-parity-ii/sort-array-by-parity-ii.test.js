import { sortArrayByParityII } from './sort-array-by-parity-ii';

describe('[Leetcode] sortArrayByParity 2', () => {
  test('example #1', () => {
    expect(sortArrayByParityII([4, 2, 5, 7])).toEqual([4, 5, 2, 7]);
  });
});
