import { singleNumber3 } from './single-number-iii';

describe('[Leetcode] singleNumber3', () => {
  test('example #1', () => {
    expect(singleNumber3([1, 2, 1, 3, 2, 5])).toEqual([3, 5]);
  });
});
